		<div class="sidebar">
	        <div id="top_filler">
        	</div>
			<div class="departments">
				<h4 id="department_header">
					<%= link_to "Departments", root_path %>
				</h4>
				<div id="department">
					<% Department.roots.each do |department| %>
    					<div>
    						<%= link_to department.department_name, department %>
    					</div>
    				<% end %>
    			</div>
    		</div>
    		<hr class="breaker"/>
    		<div class="subdepartments">
    			<h4 id="subdepartment_header">
    				<%= link_to "Browse", root_path %>
    			</h4>
				<div id="parent">
					<% unless @department.is_root? %>
						<% @department.ancestors.each do |department| %>
							<div>
								<%= link_to_unless_current department.department_name, department %>
							</div>
						<% end %>
					<% end %>
					<div>
						<%= link_to_unless_current @department.department_name %>
					</div>
				</div>
				<div id="siblings">
					<% @department.children.each do |department| %>
						<div>
							<%= link_to_unless_current department.department_name, department %>
						</div>
					<% end %>
					<% @products.each do |product| %>
						<div>
							<%= link_to_unless_current product.product_name, product %>
						</div>
					<% end %>
				</div>
			</div>
		    <div id="bottom_filler">
	        </div>	
	       	<div class="hidenames">
		        <div class="hideseller">
		        	<span class="sellerfiller">
		        	secret
		        	</br>
		        	place
		        	</br>
		        	here
		        	</br>
		        	</span>
		        </div>
				<span class="hideglance">
				<span class="gap"></span>
					<span class="onname"><%= @department.first_glance_name %></span>
					<span class="offname"><%= @department.second_glance_name %></span>
					<span class="onname"><%= @department.third_glance_name %></span>
					<span class="offname"><%= @department.fourth_glance_name %></span>
					<span class="onname"><%= @department.fifth_glance_name %></span>
				</span>
				<span class="hidefeature">
					<span class="gap"></span>
					<span class="onname"><%= @department.first_feature_name %></span>
					<span class="offname"><%= @department.second_feature_name %></span>
					<span class="onname"><%= @department.third_feature_name %></span>
					<span class="offname"><%= @department.fourth_feature_name %></span>
					<span class="onname"><%= @department.fifth_feature_name %></span>
				</span>
				<span class="hidespec">
					<span class="gap"></span>
					<span class="onname"><%= @department.first_spec_name %></span>
					<span class="offname"><%= @department.second_spec_name %></span>
					<span class="onname"><%= @department.third_spec_name %></span>
					<span class="offname"><%= @department.fourth_spec_name %></span>
					<span class="onname"><%= @department.fifth_spec_name %></span>
				</span>
			</div>
		</div>

	